<% provide(:title, "#{@bike.title}の編集") %>
<%= render "error", bike: @bike %>
<div class="container mt-5">
  <div class="row">
    <div class="col-6 mx-auto">
      <h2>編集</h2>
      <%= render "form", bike: @bike%>
    </div>
  </div>
</div>
